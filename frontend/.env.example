# Leave blank in local development to use the Vite proxy.
VITE_API_BASE_URL=
VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# Local UI fixture only. Must stay unset (or false) for any release build.
VITE_USE_MOCK_API=false
